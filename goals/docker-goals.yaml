docker_build:
  test:
  - has_file: Dockerfile

  depends_on: build

  goals:
  - dockerBuild

---

deploy:
  test:
  - and:
    - has_file: Dockerfile
    - ToDefaultBranch

  depends_on: docker_build

  goals:
  - stagingDeployment
  - productionDeployment
  - [releaseDocker, releaseTag, releaseVersion]
